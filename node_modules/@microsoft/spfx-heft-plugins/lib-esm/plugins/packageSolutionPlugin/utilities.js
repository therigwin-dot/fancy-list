export function serialAsync(list) {
    return list.reduce((previousPromise, nextPromiseFunction)=>previousPromise.then(nextPromiseFunction), Promise.resolve());
}

//#sourceMappingUrl=./utilities.js.map